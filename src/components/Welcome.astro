---
---

<div class="page">
  <nav class="topnav">
    <div class="brand">TrueAssist Technology</div>
    <div class="nav-links">
      <a href="/">Blog Home</a>
      <a href="/ramp-calculator">Ramp Calculator</a>
      <a href="https://www.trueassistech.com/" target="_blank" rel="noopener">Main Site</a>
      <a class="nav-btn" href="mailto:info@trueassistech.com">Contact Us</a>
    </div>
  </nav>

  <header class="hero">
    <div class="hero-bg"></div>
    <div class="hero-content">
      <div class="eyebrow">AI-powered assistance</div>
      <h1>Smarter workflows with trusted AI delivery</h1>
      <p class="lede">
        We build automation and assistive experiences that stay transparent, performant, and trustworthy—so your teams can move faster with confidence.
      </p>
      <div class="cta">
        <a class="btn primary" href="https://www.trueassistech.com/" target="_blank" rel="noopener">Visit main site</a>
        <a class="btn ghost" href="#latest-posts">Read the blog</a>
      </div>
      <div class="chips">
        <span>Automation &amp; AI</span>
        <span>Data transparency</span>
        <span>Reliable delivery</span>
      </div>
    </div>
    <div class="hero-card">
      <p class="eyebrow">Latest post</p>
      <h3>Ramp Calculator</h3>
      <p class="muted">Size ramps safely with slope guidance and instant outputs.</p>
      <a class="btn primary full" href="#latest-posts">Open post</a>
    </div>
  </header>

  <section class="feature" id="latest-posts">
    <div>
      <p class="eyebrow">What we're sharing</p>
      <h2>Product notes &amp; practical guides</h2>
      <p class="muted">Updates, implementation tips, and reliable patterns for assistive AI systems.</p>
      <div class="points">
        <div class="point">
          <span class="dot"></span>
          <div>
            <strong>Ramp Calculator</strong>
            <p class="muted">Quickly calculate ramp length from rise and slope ratios with clear safety cues.</p>
          </div>
        </div>
        <div class="point">
          <span class="dot"></span>
          <div>
            <strong>Transparent delivery</strong>
            <p class="muted">Observability, testing, and on-call ownership keep releases predictable.</p>
          </div>
        </div>
        <div class="point">
          <span class="dot"></span>
          <div>
            <strong>Human-centered AI</strong>
            <p class="muted">Guardrails, review points, and clear outcomes guide every launch.</p>
          </div>
        </div>
      </div>
    </div>
    <div class="card">
      <p class="eyebrow">Get in touch</p>
      <h3>Work with TrueAssist</h3>
      <p class="muted">Have a feature request or want a demo? Reach out and we'll tailor it to your use case.</p>
      <a class="btn primary full" href="mailto:info@trueassistech.com">Contact us</a>
    </div>
  </section>
</div>

<style>
  .page { max-width: 1180px; margin: 0 auto; padding: 28px 22px 96px; }

  .topnav {
    display: flex; align-items: center; justify-content: space-between;
    padding: 12px 16px; background: rgba(255, 255, 255, 0.85);
    border: 1px solid var(--border); border-radius: 14px;
    box-shadow: 0 14px 30px rgba(59, 31, 95, 0.12); backdrop-filter: blur(6px);
  }
  .brand { font-weight: 800; letter-spacing: -0.02em; color: var(--primary-2); }
  .nav-links { display: flex; align-items: center; gap: 14px; flex-wrap: wrap; }
  .nav-links a {
    color: var(--text); font-weight: 600; text-decoration: none; padding: 8px 10px;
    border-radius: 10px; transition: color 140ms ease, background 140ms ease;
  }
  .nav-links a:hover { color: var(--primary-2); background: rgba(59, 31, 95, 0.08); }
  .nav-btn { background: linear-gradient(135deg, var(--primary), var(--primary-2)); color: #fff; box-shadow: 0 10px 20px rgba(59, 31, 95, 0.25); }

  .hero {
    position: relative; margin-top: 24px; padding: 46px; border-radius: 22px; overflow: hidden;
    background: linear-gradient(135deg, rgba(59, 31, 95, 0.96), rgba(76, 43, 118, 0.94)); color: #fff;
    box-shadow: 0 24px 60px rgba(59, 31, 95, 0.22); display: grid; grid-template-columns: 1.4fr 0.9fr; gap: 28px;
  }
  .hero-bg {
    position: absolute; inset: 0;
    background: radial-gradient(circle at 20% 20%, rgba(124, 81, 161, 0.35), transparent 40%),
      radial-gradient(circle at 80% 0%, rgba(255, 255, 255, 0.18), transparent 45%);
    pointer-events: none;
  }
  .hero-content { position: relative; z-index: 1; }
  .hero h1 { font-size: clamp(32px, 4vw, 44px); margin: 0.15em 0 0.35em; letter-spacing: -0.02em; }
  .lede { max-width: 740px; font-size: 18px; margin: 0 0 1.15em; color: rgba(255, 255, 255, 0.92); }

  .cta { display: flex; gap: 12px; flex-wrap: wrap; margin-bottom: 16px; }
  .btn { display: inline-flex; align-items: center; justify-content: center; padding: 12px 18px; border-radius: 12px; font-weight: 700; border: 1px solid transparent; text-decoration: none; transition: transform 120ms ease, box-shadow 160ms ease, background 160ms ease; }
  .btn.primary { background: #fff; color: var(--primary); box-shadow: 0 12px 30px rgba(0, 0, 0, 0.18); }
  .btn.primary:hover { transform: translateY(-1px); box-shadow: 0 14px 32px rgba(0, 0, 0, 0.22); }
  .btn.ghost { border-color: rgba(255, 255, 255, 0.35); color: #fff; background: rgba(255, 255, 255, 0.12); }
  .btn.ghost:hover { border-color: rgba(255, 255, 255, 0.5); background: rgba(255, 255, 255, 0.18); }
  .btn.full { width: 100%; justify-content: center; }

  .chips { display: flex; flex-wrap: wrap; gap: 10px; margin-top: 6px; }
  .chips span { padding: 8px 12px; background: rgba(255, 255, 255, 0.12); border: 1px solid rgba(255, 255, 255, 0.2); border-radius: 999px; font-weight: 600; color: #f3eafa; backdrop-filter: blur(4px); }

  .hero-card {
    position: relative; z-index: 1; background: #ffffff; color: var(--text);
    padding: 22px; border-radius: 16px; box-shadow: 0 18px 46px rgba(59, 31, 95, 0.22);
    align-self: center;
  }
  .hero-card h3 { margin: 6px 0 8px; }

  .eyebrow { text-transform: uppercase; letter-spacing: 0.08em; font-size: 12px; font-weight: 700; color: #cbb8e7; margin: 0 0 6px; }
  .muted { color: var(--muted); margin: 0 0 12px; }

  .feature {
    margin-top: 32px; background: #ffffff; border-radius: 18px; padding: 28px;
    display: grid; grid-template-columns: 1.6fr 1fr; gap: 22px;
    border: 1px solid var(--border); box-shadow: 0 16px 40px rgba(59, 31, 95, 0.1);
  }
  .feature h2 { margin: 2px 0 10px; font-size: 26px; letter-spacing: -0.01em; color: var(--text); }
  .points { display: flex; flex-direction: column; gap: 12px; margin-top: 12px; }
  .point { display: flex; gap: 10px; align-items: flex-start; }
  .dot { width: 12px; height: 12px; border-radius: 50%; margin-top: 6px; background: linear-gradient(135deg, var(--primary), var(--accent)); box-shadow: 0 4px 12px rgba(59, 31, 95, 0.35); }

  .card {
    align-self: center; background: linear-gradient(145deg, var(--primary) 0%, var(--primary-2) 55%, var(--accent) 100%);
    color: #ffffff; padding: 24px; border-radius: 16px; box-shadow: 0 16px 36px rgba(59, 31, 95, 0.2);
  }
  .card h3 { margin: 4px 0 8px; font-size: 22px; }

  @media (max-width: 1024px) { .hero { grid-template-columns: 1fr; } }
  @media (max-width: 840px) {
    .feature { grid-template-columns: 1fr; }
    .topnav { flex-direction: column; align-items: flex-start; gap: 8px; }
  }
  @media (max-width: 640px) {
    .hero { padding: 32px 24px; }
    .page { padding: 22px 16px 68px; }
  }
</style>
